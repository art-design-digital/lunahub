[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "ghostscript", "poppler_utils", "binutils", "imagemagick"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node build/index.js"
